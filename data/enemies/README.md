# Enemy Data

Place `EnemyDef` `.tres` resources here.

MVP ids:

* `tactichandal_runner`
* `lagrima_negra`
* `resonante_bruiser`
* `clan_loot_summoner`
* `killo_bulevar_boss`

Do not add non-MVP enemies until the base loop is playable.
